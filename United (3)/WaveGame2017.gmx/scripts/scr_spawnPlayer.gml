//ref to the menu
audienceManager = instance_find(obj_ctrl_audienceManager, 0);

var p = ds_stack_pop(audienceManager.population);
p.walking = true;
var p = ds_stack_pop(audienceManager.population);
p.walking = true;
var p = ds_stack_pop(audienceManager.population);
p.walking = true;
var p = ds_stack_pop(audienceManager.population);
p.walking = true;
var p = ds_stack_pop(audienceManager.population);
p.walking = true;
